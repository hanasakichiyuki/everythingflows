# 万物流转：UI/UX 与产品迭代方案

> 状态：方案待确认，尚未执行 UI/业务代码改造。  
> 更新日期：2026-07-25  
> 关键决策：**网站端移除 Live2D；现有 Live2D 引擎代码暂时保留，作为未来独立桌面陪伴应用的可迁移基础。**

## 1. 产品定位与设计方向

项目应定位为“可沉浸阅读、记录碎片、进行 AI 对话的个人数字花园”。博客是核心内容，碎片、音乐和 AI 是增强体验的模块；它不应演变为高密度桌面仪表盘。

参考项目 [end4-pC](https://github.com/pctrade/end4-pC) 是 Quickshell/Hyprland 桌面 Shell，而不是网页。适合学习它的模块化卡片、色调化表面、分层设置面板和克制的状态反馈；不适合复制其自由悬浮、桌面级信息密度和多栏常驻布局。

设计目标：

- 访客在 30 秒内理解站点、发现文章并进入阅读。
- 阅读页面保持安静、专注，氛围元素不遮挡内容。
- 首页以文章发现为主，碎片、音乐和 AI 为次级入口。
- 全站以同一套颜色、间距、圆角、交互和可访问性规则构建。

## 2. 当前项目分析

### 2.1 技术与架构

- Next.js 16 App Router、React 19、TypeScript 5、Tailwind CSS。
- next-intl（当前仅中文）、next-themes。
- Supabase 承担文章、碎片、鉴权、存储与聊天数据；数据访问必须继续通过 `src/lib/api/` 抽象层。
- Novel + TipTap 2 为后台编辑器，新文章以 TipTap JSON 为规范内容。
- AI 聊天使用 Vercel AI SDK，支持多 Provider 和匿名免费模型。
- 现有音乐播放器、时间线、碎片墙、Giscus、自定义编辑器均为受保护功能。

目录职责清晰：`src/app` 管路由/API/Actions，`src/components` 按 layout、blog、memory、chat、admin、ui 分域，`src/lib` 管数据、编辑器、AI 与 Live2D，`supabase` 存放需手动执行的 Schema 与迁移。

### 2.2 现有页面与组件

- 公共页面由 `MainLayout` 承载：固定侧栏、背景图、玻璃 Surface、搜索、音乐播放器。
- 首页由 Hero、最新文章、碎片预览组成。
- 文章页具备正文、标签、上下篇和评论；归档采用时间线。
- 碎片墙采用瀑布流和详情弹窗。
- 聊天是独立会话界面，支持模型选择、流式回复与匿名本地历史。
- 后台采用写作工作台，具备自动保存、AI 写作、图片和丰富文本能力。

### 2.3 优点

- API 抽象层、公共读/管理写分离、ISR 和 Server Components 使用合理。
- 已有 `Surface`、`Button`、`Dialog` 等基础组件及深浅色令牌。
- 动画、焦点态、跳过导航和移动播放器安全区已有良好基础。
- 音乐、碎片、AI、编辑器均是真实可用功能，不是仅展示原型。

### 2.4 问题摘要

1. **信息架构偏重**：固定 200px 侧栏同时承载个人资料、社交、导航、搜索和登录；移动端缺少持续可见的页面导航。
2. **首页内容发现不足**：Hero 主要展示诗句和日期，缺少精选文章、分类、标签和明确入口；最新文章与碎片权重相同，弱化博客定位。
3. **视觉语言分裂**：公共页使用粉色玻璃风，Hero 使用青色，碎片墙固定深色 Zinc，后台又有绿色状态语义。
4. **组件重复与边界过大**：搜索、归档列表存在重复实现；`PostEditor`、`useChatState` 等组件职责过重；右栏 Context 为未完成残留架构。
5. **性能与体验风险**：全局布局为所有页面加载搜索索引；背景、模糊和环境组件需要更明确的性能预算；碎片图片缺少统一媒体策略。
6. **一致性与可访问性不足**：业务组件中仍有大量硬编码色阶和小字号；自制抽屉/弹层需补齐键盘与焦点管理；缺少 UI/E2E/响应式回归测试。
7. **文档与行为不一致**：匿名聊天实际使用 localStorage 持久化，README 的描述需要统一。

## 3. Live2D 决策

### 网站端：移除

网站核心任务是阅读与内容探索。Live2D 在桌面端会与左侧导航、音乐播放器和文章内容竞争注意力及屏幕空间；模型渲染成本也不适合成为博客公共体验的一部分。

网站改造后：

- 不再在公共布局中挂载 Live2D。
- 不再将 Live2D 作为首屏或阅读页氛围元素。
- 音乐播放器保留，但新增“安静阅读模式”以便用户最小化环境干扰。
- 在确认独立项目启动前，保留 `src/lib/live2d/` 代码，不立即删除。

### 未来桌面端：独立项目

Live2D 更适合作为独立桌面陪伴应用，能力可以包括透明悬浮、始终置顶、托盘控制、模型切换、鼠标交互、时间/音乐/专注状态反馈。现有引擎、状态机、台词和互动逻辑可作为迁移来源；网站与桌面项目之间只保留可选的音乐/状态通信，不耦合页面渲染。

## 4. UI 优化路线图

### 4.1 目标布局

桌面端采用“紧凑导航轨道 + 内容区 + 按页面出现的辅助模块”。首页在大屏可用文章主列配合碎片/状态辅助列；文章页保持约 720px 的阅读宽度，不常驻杂项卡片。

移动端采用“固定 Header + 底部主导航 + 播放器安全区”。主入口固定为首页、归档、碎片、AI 和更多；搜索通过命令入口打开。

### 4.2 具体改造

| 改造项 | 原因 | 预期收益 | 主要文件 |
|---|---|---|---|
| 设计令牌与组件原语 | 颜色、圆角、阴影、焦点态分散 | 全站一致，改主题更快 | `globals.css`、`tailwind.config.ts`、`components/ui/*` |
| 公共壳层与导航 | 固定侧栏拥挤，移动端导航弱 | 更清晰的信息架构和响应式体验 | `MainLayout.tsx`、`Sidebar.tsx`、新增 PublicHeader/MobileNavigation |
| 首页重构 | 内容发现不足 | 提升文章点击与回访 | `HomePageContent.tsx`、`LatestPosts.tsx`、`Fragments.tsx` |
| 文章与发现页 | 阅读辅助和筛选能力弱 | 提升阅读完成率和内容探索 | blog 页面、Archive、Search |
| 卡片、列表、表格统一 | 重复 JSX 和不同交互样式 | 更低维护成本 | PostCard、ArchiveTimeline、Drafts、ui 原语 |
| 碎片墙与详情链接 | 深浅色不一致、无法直接定位 | 可分享、可访问、更稳定的图片体验 | MemoryWall、MemoryCard、FragmentDetailModal |
| 聊天工作区优化 | 会话列表为浮层，空状态薄弱 | 提升持续对话效率与首次使用体验 | ChatShell、ConversationList、ModelPicker |
| 后台编辑器拆分 | 设置和编辑在小屏拥挤，组件过大 | 更好的写作效率和可维护性 | PostEditor、EditorMenus、AdminWorkspaceShell |
| 环境层收敛 | 音乐/浮动操作可能遮挡内容 | 更专注的阅读体验 | MusicPlayer、MainLayout、AmbientLayerContext |
| 搜索与性能治理 | 全站下发搜索索引，废弃右栏状态存在 | 更小的客户端负担 | locale layout、SearchModal、RightSidebarContext |

### 4.3 视觉规范

- 以暖中性画布为底，玫瑰色为主品牌色，青绿色仅作为辅助色。
- UI 和文章正文使用 Geist/系统中文字体；诗句、引用和碎片使用 LXGW WenKai。
- 正文 16～18px；普通 UI 不低于 12px；重要操作不低于 14px。
- 使用 4px 间距网格，常用值为 8、12、16、24、32、48、64。
- 控件圆角 10～12px，卡片圆角 18～22px，胶囊仅用于状态/筛选。
- 动画以 120/180/240/320ms 为节奏；禁止通过改变 padding 制造 hover 位移；遵守 `prefers-reduced-motion`。
- 所有新交互满足键盘可达、焦点可见、44×44px 触控面积和 WCAG AA 对比度。

## 5. 产品功能路线图

### P0：必须做

| 功能 | 用户价值 | 难度 | 建议方案 |
|---|---|---:|---|
| 统一内容发现中心 | 快速找到文章、标签和碎片 | 中 | 提取共享搜索 Hook，按需加载索引；数据量增长后再接 Supabase FTS |
| 写作预览与崩溃恢复 | 降低误发布和内容丢失风险 | 中 | 复用文章渲染器做预览；用 IndexedDB/localStorage 存临时快照 |
| 碎片独立链接与分享 | 让每条碎片成为可访问内容 | 中 | 新增 `/fragments/[id]`，继续经 `src/lib/api/fragments.ts` 查询 |
| 聊天引导与状态透明 | 明确模型、历史、保存和失败状态 | 中 | 提示词卡片、模型说明、保存状态、重试与统一错误处理 |
| 安静阅读模式 | 阅读时减少环境干扰 | 低～中 | Context + localStorage，最小化音乐/导航，不删除受保护功能 |

### P1：重要功能

| 功能 | 用户价值 | 难度 | 建议方案 |
|---|---|---:|---|
| 系列、专题、相关文章 | 建立连续阅读路径 | 中 | 先复用分类/标签，后续再增加 series 数据模型 |
| RSS/Atom 与订阅 | 提升老访客回访 | 低～中 | Route Handler 生成 Feed，订阅服务做 Provider 抽象 |
| 向博客提问 | 将 AI 与站内内容结合 | 高 | 内容切片、Embedding、pgvector、引用来源和严格限流 |
| 版本历史、定时发布、SEO 封面 | 提升写作与发布能力 | 高 | 新表/字段以手动 SQL 迁移方式引入 |
| 主题预设与壁纸色调 | 提升个性化 | 中 | 优先做手动主题预设；动态取色必须通过对比度校验 |

### P2：长期规划

| 功能 | 用户价值 | 难度 | 建议方案 |
|---|---|---:|---|
| 知识图谱和双向链接 | 可视化内容关系 | 高 | 引用解析、图谱视图、渐进加载 |
| 收藏、阅读历史、跨设备同步 | 长期使用体验 | 高 | 先 localStorage，登录后再同步 Supabase |
| 可配置首页组件 | 个性化首页内容优先级 | 高 | Widget Registry + 配置 Schema，避免自由悬浮布局 |
| PWA 离线阅读 | 提升移动端稳定性 | 中～高 | Manifest、公开文章缓存、版本化 Service Worker |
| 多语言内容 | 覆盖更多读者 | 高 | 利用 next-intl，内容翻译关联通过手动迁移引入 |

## 6. AGENTS.md 补充建议

现有数据抽象、手动迁移、Server Component 优先和受保护功能规则均应保留。建议新增 UI 规范：

- 业务组件不得直接硬编码品牌色、圆角、阴影与 z-index。
- 公共交互优先复用 `src/components/ui/` 原语。
- 所有新页面必须验证键盘、焦点、Reduced Motion、对比度及 320/375/768/1024/1440px。
- 新数据能力继续通过 `src/lib/api/`；数据库变更只提交迁移文件，不自动执行。
- 公共文案进入 next-intl，避免新增散落的硬编码文本。
- UI 大改需要视觉回归或 Playwright 响应式测试。
- 仓库当前使用 npm/package-lock；如需迁移 pnpm，应独立完成，不与 UI 改造混合。

## 7. 推荐执行顺序

1. 为当前未提交改动建立独立 Git 检查点。
2. 完成设计令牌、基础 UI 原语和公共导航壳层。
3. 重构首页，确认新的视觉和内容优先级。
4. 扩展到文章页、归档、标签和搜索。
5. 优化碎片墙、聊天和音乐的环境层；网站端移除 Live2D 挂载。
6. 拆分后台编辑器，补齐预览、恢复和草稿管理。
7. 进行可访问性、性能、移动端和视觉回归收尾。

第一批建议只实施“设计系统 + Header/Sidebar/移动导航 + 首页”，不触及数据表结构。确认视觉方向后再进入后续页面与功能迭代。
