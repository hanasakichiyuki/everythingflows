# 万物流转：UI/UX 与产品迭代计划

> 更新日期：2026-07-27
> 当前状态：第一阶段、核心阅读链路、碎片体验升级、AI 聊天/音乐体验统一、登录与后台编辑器体验收口，以及状态组件、可访问性基础收口和端到端回归均已完成代码实施，现处于视觉确认；后台回归直接使用当前 Supabase 项目，但临时账号和文章会在测试结束后自动清理。

## 0. 已确认的产品与设计决策

- 网站定位从强调“数字花园”调整为更开放的“个人内容空间”：以文章阅读为核心，碎片、音乐和 AI 对话作为辅助能力。
- 不刻意使用花园、植物等具象意象；可保留少量抽象的流动线条，呼应“万物流转”，但不让装饰压过内容。
- 品牌主色采用偏冷的青蓝色，玫红只用于少量强调、状态和情绪点缀，避免页面整体过绿或过粉。
- 首页参考桌面工作台的清晰分区与卡片层级，但不照搬高密度仪表盘，仍需保持个人博客的安静感和阅读感。
- 网站端不再挂载 Live2D；相关引擎代码暂时保留，后续更适合独立为桌面 Live2D 项目。
- 导航中的“个人网站”入口已经隐藏，减少语义重复和无效跳转。
- 不引入虚假的天气、心情、播放数据或统计数据；首页展示必须来自现有真实内容和真实功能。

---

## 1. 当前项目分析

### 1.1 技术栈

- Next.js 16 App Router、React 19、TypeScript 5。
- Tailwind CSS 与 `globals.css` CSS Variables 共同承担样式和主题系统。
- `next-themes` 提供明暗主题切换。
- `next-intl` 负责国际化，目前仅启用中文且 URL 不携带 locale。
- Supabase 提供文章、碎片、认证、图片存储和 AI 会话数据。
- Novel、TipTap 2 提供文章编辑器与结构化内容格式。
- MetingJS 与 APlayer 提供网易云音乐播放能力。
- Vitest 与 ESLint 用于测试及静态检查。

### 1.2 目录与职责

| 目录 | 当前职责 | 约束与后续方向 |
| --- | --- | --- |
| `src/app/[locale]/` | 首页、博客、归档、碎片、聊天、登录、后台等路由 | 页面继续优先使用 Server Components |
| `src/components/layout/` | 公共壳层、侧栏、页头、首页模块、音乐播放器 | 已成为本轮 UI 改造核心，后续应避免继续堆叠页面业务逻辑 |
| `src/components/ui/` | 通用 Surface 等基础 UI | 需要逐步补齐按钮、标签、空状态、骨架屏等通用原语 |
| `src/lib/api/` | 页面与 Supabase 之间的数据抽象层 | 必须保留；页面和组件不可直接绕过该层访问数据 |
| `src/lib/api/supabase/` | Supabase 实现 | 数据源替换细节应限制在这里 |
| `src/lib/editor/` | TipTap schema、扩展与转换逻辑 | 继续保证 TipTap JSON 是新文章的规范数据源 |
| `src/config/` | 网站信息、导航、音乐及功能开关 | 导航和产品能力开关应尽量从此处驱动 |
| `src/hooks/` | 时间、音乐播放器等客户端状态 | 保持单一职责，避免页面直接复制状态逻辑 |

### 1.3 当前公共页面骨架

当前公共页面已从“全屏背景图 + 玻璃拟态浮层”调整为更稳定的应用式骨架：

- 桌面端使用约 220px 固定侧栏，内容区顶部提供搜索、主题切换等全局操作。
- 移动端使用轻量顶部栏与固定底部导航，补齐了此前移动端缺少稳定导航的问题。
- 首页内容宽度放宽，以容纳主内容和右侧辅助栏；文章阅读页仍应维持更窄的阅读宽度。
- 博客、归档、搜索、标签和碎片等集合页使用约 1280px 的宽内容区，并移除整页大卡片；文章与单条详情继续保持聚焦宽度。
- 页面背景改为暖中性色，不再依赖全屏壁纸，降低图片加载、文字对比度和视觉噪音风险。
- Live2D 已不再由公共布局挂载，避免遮挡内容、增加首屏资源和干扰移动端交互。

### 1.4 当前首页组成

首页目前已经形成清晰的三层结构：

1. 品牌与主要行动
   - `Everything / Flows` 主标题。
   - 简短诗句表达网站气质。
   - “阅读最新文章”和“浏览全部碎片”两个主要入口。
   - 抽象青蓝流线插画，仅作为氛围和品牌识别辅助。

2. 核心内容
   - 最新文章作为 Featured 内容突出展示。
   - 碎片采用图文宫格，承担更轻、更即时的内容预览。
   - 文章列表以缩略图、摘要、日期和阅读时长形成可扫描结构。

3. 辅助能力
   - 当前时间与日期。
   - 接入真实播放器状态的音乐卡片。
   - AI 对话入口。
   - 真实文章、碎片等内容统计。

该结构已经解决旧首页信息量不足的问题，同时没有加入无法落地的天气、心情等装饰性假功能。

### 1.5 当前视觉规范

#### 色彩

| 角色 | 建议值 | 使用原则 |
| --- | --- | --- |
| 页面背景 | `#F7F3EC` | 暖中性画布，避免纯白刺眼 |
| 主要文字 | `#202A2E` | 标题、正文和高优先级信息 |
| 次要文字 | `#6F797D` | 描述、元信息和辅助说明 |
| 品牌主色 | `#208CAB` | 链接、主按钮、选中态、关键图标 |
| 品牌主色浅底 | `rgba(32, 140, 171, 0.1)` | 导航选中背景、弱提示和 hover |
| 辅助玫红 | `#C35B78` | 少量关键词、情绪强调和进度状态 |
| 深色主题主色 | `#69CFDF` | 保证深色背景下的识别度与对比度 |
| 深色主题辅色 | `#EF8FAA` | 深色模式中的少量强调 |

使用比例建议为：中性色约 80%，青蓝约 15%，玫红不超过 5%。玫红不能与青蓝同时大面积竞争视觉焦点。

#### 字体与层级

- 页面大标题可保留衬线字体，表达个人内容空间的编辑感。
- 导航、按钮、标签、正文和数据使用易读的无衬线字体。
- 正文宽度控制在约 65–75 个中文字符的舒适范围内。
- 首页 Hero 标题只负责品牌识别，不和文章标题争夺同一层级。

#### 间距与圆角

- 基础间距采用 4 / 8 / 12 / 16 / 24 / 32 / 48 的节奏。
- 卡片内部默认 20–24px；移动端缩减至 16–20px。
- 卡片圆角建议 16–20px，按钮圆角可更完整，但不要所有组件都做成胶囊。
- 阴影保持轻量，以边框、背景层次和留白为主。

---

## 2. 已落地改造与收益

| 改造项 | 状态 | 修改原因 | 当前收益 | 主要涉及文件 |
| --- | --- | --- | --- | --- |
| 统一青蓝主色与玫红辅色 | 已完成 | 旧界面存在粉色、青色和壁纸色彩竞争 | 品牌识别更稳定，页面不再过绿或过粉 | `src/app/globals.css`、`src/components/ui/surface.tsx` |
| 移除全屏壁纸依赖 | 已完成 | 壁纸影响文字可读性和内容层级 | 页面更安静，加载和主题适配更可控 | `src/components/layout/MainLayout.tsx` |
| 重构桌面侧栏 | 已完成 | 旧导航密度和选中反馈不足 | 导航更清晰，同时保留用户偏好的左侧栏形式 | `src/components/layout/Sidebar.tsx` |
| 新增桌面公共页头 | 已完成 | 搜索和主题操作缺少固定入口 | 全局操作位置稳定，内容区起点统一 | `src/components/layout/PublicHeader.tsx` |
| 新增移动端底部导航 | 已完成 | 移动端跨页面成本高 | 主要功能可以单手快速到达 | `src/components/layout/MobileNavigation.tsx` |
| 首页重新编排 | 已完成，待视觉确认 | 旧首页内容层级偏弱、空间利用不足 | 品牌、文章、碎片、音乐和 AI 形成主次分明的结构 | `src/components/layout/HomePageContent.tsx` |
| 文章与碎片卡片升级 | 已完成首页部分 | 列表扫描效率不足 | 标题、摘要、图片和元信息更容易浏览 | `LatestPosts.tsx`、`Fragments.tsx` |
| 音乐播放器首页卡片化 | 已完成 | 浮动播放器与首页布局割裂 | 首页展示真实音乐能力，其他页面仍保留浮动形态 | `MusicPlayer.tsx`、`HomeMusicCard.tsx` |
| 网站端停止挂载 Live2D | 已完成 | 对内容网站价值有限，且增加性能与干扰 | 首屏更轻，移动端和阅读场景更专注 | `src/components/layout/MainLayout.tsx` |
| 隐藏“个人网站”入口 | 已完成 | 与当前站点语义重复 | 导航更简洁，减少无效选择 | `src/config/site.ts` |
| 新增博客列表与标签筛选 | 已完成，待视觉确认 | 原站点缺少独立浏览入口，文章发现依赖首页和归档 | 建立标签筛选、分页与统一空状态的阅读入口 | `src/app/[locale]/blog/page.tsx`、`BlogList.tsx`、`PostCard.tsx` |
| 文章详情阅读层级收敛 | 已完成，待视觉确认 | 正文宽度、元信息和翻页卡片与新公共壳层割裂 | 阅读宽度稳定在约 720px，阅读路径更专注 | `src/app/[locale]/blog/[slug]/page.tsx`、`PostNavigation.tsx` |
| 归档时间线升级 | 已完成，待视觉确认 | 原时间线强调色偏粉，月份关系不明确 | 年/月/日层级更清晰，移动端扫描更稳定 | `ArchiveTimeline.tsx`、`archive/page.tsx` |
| 搜索按需加载与结果升级 | 已完成，待视觉确认 | 所有公共页面预加载完整索引，搜索结果信息不足 | 弹层打开后请求索引，提供加载、失败、高亮、类型和键盘导航 | `api/search/route.ts`、`SearchModal.tsx`、`SearchResultList.tsx` |
| 集合页宽度与层级统一 | 已完成，待视觉确认 | 非首页页面统一限制为 4xl，且存在卡片套卡片 | 列表页放宽至 1280px 并使用无整页卡片画布，内容卡片尺寸和首页更接近 | `MainLayout.tsx`、`surface.tsx`、博客/归档/搜索页面 |
| 碎片详情、复制链接与图片降级 | 已完成，待视觉确认 | 单条碎片无法定位分享，图片失败会留下破图或永久骨架 | 提供 `/fragments/[id]`、稳定的复制链接、统一加载和失败状态；总页恢复无外层卡片的 2–5 列瀑布流 | `fragments/[id]/page.tsx`、`FragmentDetail.tsx`、`FragmentImage.tsx` |
| AI 聊天与音乐状态统一 | 已完成，待视觉确认 | 匿名限制、历史加载和音乐外部服务状态缺乏一致反馈 | 聊天明确匿名能力、模型、错误与重试；音乐在首页、桌面和移动端共享加载、空列表、失败、初始化与切歌状态 | `src/components/chat/`、`useChatState.ts`、`useMusicPlayer.ts`、`MusicPlayer*.tsx` |

---

## 3. 当前仍需解决的问题

### 3.1 全站一致性尚未完成

首页、博客、文章详情、归档、搜索、碎片和聊天已经进入新视觉体系；登录和后台工作台现已使用同一套 Surface、青蓝主色和内容层级。基础键盘焦点、弹层关闭与焦点返回、语义状态和减少动效偏好已经补齐，仍需在真实浏览器尺寸下做视觉确认，避免编辑器长内容、窄屏和深色主题出现局部割裂。

下一阶段优先进行真实设备与明暗主题的人工验收；后台发布自动化验证已直接使用当前 Supabase 项目，并在每次测试中创建、验证和删除临时账号与临时文章，不触及已有内容。

### 3.2 首页稳定文案已迁移

Hero 标题、诗句、按钮文案和辅助卡片文案已迁入 `messages/zh.json`。后续新增用户可见的稳定文案应继续遵循这一规范，避免组件重新承担内容配置职责。

### 3.3 Featured 文章表现依赖现有数据

当前首篇文章承担 Featured 角色，但文章数据若没有封面、摘要或阅读时长，卡片表现会退化。需要为缺省数据设计统一 fallback，并明确是否在文章模型中增加“推荐”语义；若暂时不改数据库，可继续使用“最新已发布文章”作为确定性规则。

### 3.4 碎片独立访问与分享已完成收口

碎片总页和首页预览卡均已直达 `/fragments/[id]`。详情页提供稳定的复制链接，并保留经过认证后的真实编辑和删除操作；创建、更新、删除会同步失效列表、详情页与站点地图缓存。

### 3.5 搜索资源加载策略已完成收口

公共布局不再预取完整索引；弹层打开时通过 `/api/search` 请求缓存索引，并具备加载、空结果、错误、高亮和键盘导航状态。直达 `/search` 页面仍使用服务端索引，保证无 JavaScript 时的基本可用性。

### 3.6 外部资源稳定性仍是体验风险

Supabase 图片、MetingJS 和网易云资源都受网络、代理和图片域名策略影响。碎片图片现在具有统一的加载与失败占位，且使用浏览器原生图片加载以绕过服务端图片代理限制；歌单和播放器仍需要补齐相同等级的失败状态。

### 3.7 缺少视觉回归与关键交互测试

当前单元测试、类型检查和 lint 可以覆盖代码质量，但无法捕捉导航遮挡、移动端溢出、播放器与底栏重叠、主题切换闪烁等视觉问题。建议补充 Playwright 关键路径和桌面/移动端截图回归。

---

## 4. 页面优化路线图

### 阶段 A：完成首页视觉确认与基础收口

状态：代码实施与工程验证已完成，待产品视觉确认。

1. 依据真实内容检查首页极端状态：无文章、无碎片、长标题、无封面、音乐加载失败。
2. 检查 320、375、768、1024、1440px 下的断点、触控区域和滚动遮挡。
3. 将 Hero 和辅助卡片中的稳定文案迁移到 i18n 消息文件。
4. 检查青蓝与玫红使用比例，清除遗留的大面积绿色、粉色和不必要渐变。
5. 确认首页右栏只承担辅助信息，不扩张成高密度仪表盘。

预期收益：把当前首页从“视觉稿已实现”提升到“真实数据和不同设备下可稳定使用”。

### 阶段 B：统一博客、文章详情、归档与搜索

状态：代码实施与工程验证已完成，待产品视觉确认。

优先文件：

- `src/app/[locale]/blog/`
- `src/app/[locale]/archive/`
- 文章详情相关组件
- 搜索弹层与搜索数据加载逻辑
- 复用的文章卡片、元信息和空状态组件

具体计划：

- 博客列表沿用首页文章列表的层级，但增加标签筛选、分页和更完整的空状态。
- 文章详情保持 720px 左右的正文宽度，减少侧栏和播放器对沉浸阅读的干扰。
- 归档页突出年份、月份和时间轴关系，避免把所有文章做成重复的大卡片。
- 搜索结果增加关键词高亮、类型标识、无结果建议与键盘导航。
- Header 搜索在用户触发后再加载索引，降低公共布局负担。

预期收益：打通“首页发现—搜索/归档筛选—文章阅读”的核心路径。

### 阶段 C：碎片体验升级

状态：代码实施与工程验证已完成，待产品视觉确认。

优先文件：

- `src/app/[locale]/fragments/`
- `src/components/layout/Fragments.tsx`
- `src/lib/api/posts.ts` 或新增保持同层抽象的 fragments seam

具体计划：

- 为每条碎片提供稳定链接或可深链接的详情状态。
- 保留瀑布流/宫格的轻量感，但统一图片比例、加载占位和失败状态。
- 支持按时间或标签浏览；移动端优先保证阅读和返回位置恢复。
- 若增加点赞或互动，必须先明确登录、匿名和防滥用策略，不在纯 UI 阶段引入假交互。

预期收益：碎片从首页装饰内容变为可访问、可分享、可持续沉淀的内容类型。

### 阶段 D：AI 聊天与音乐体验统一

状态：代码实施与工程验证已完成，待产品视觉确认。

具体计划：

- 聊天页明确展示匿名限制、当前模型、登录后的能力变化和错误原因。
- 对流式输出、重试、停止生成、历史加载建立统一状态组件。
- 音乐播放器补齐外部服务失败、无歌曲、切歌中和移动端展开状态。
- 首页音乐卡片保持真实播放器的一个视图，不另建第二套播放状态。

预期收益：减少“能不能用、为什么不能用”的不确定性，强化现有特色能力。

### 阶段 E：后台编辑器与系统化质量建设

状态：登录页、后台工作台、草稿箱、编辑器可靠性、基础状态组件与首批 Playwright 回归已完成，待视觉确认。

本轮已完成：

- 登录页改为清晰的管理入口和表单内错误反馈，不再使用遮挡页面的全宽错误横幅。
- 后台壳层、标题栏和草稿箱统一为青蓝主色、暖中性 Surface 与更稳定的列表层级。
- 编辑器加入浏览器离开提醒、保存中的串行保护、最近保存时间和保存期间继续修改的明确提示。
- 首次保存后将临时编辑状态切换为可持续更新的文章 ID，避免连续保存意外重复创建文章。
- 发布改为先在弹层中以读者视角预览，确认后再提交；旧版 MDX 文章明确展示源码预览限制，避免产生错误的“所见即所得”预期。
- 工具栏、粘贴和拖放图片上传失败后展示可关闭的错误提示和重试操作；批量上传会保留已成功插入的图片，只重试失败项。
- 补齐并开始复用 `Badge`、`EmptyState`、`Skeleton`、`ErrorState`、`Toast` 与既有 Dialog/Button；加载、空数据、失败和成功反馈不再各自实现。
- 新增 Playwright 生产模式基线：独立使用 `.next-e2e` 与 3001 端口，不和 3000 开发服务争用 `.next` 锁；覆盖首页、搜索、博客、登录入口、移动导航及首页/移动博客截图。

本轮新增完成：

- 补齐聊天能力提示的状态语义、历史面板的 `Escape` 关闭与焦点返回；修复 i18n 中重复 `chat` 对象覆盖当前聊天文案的问题。
- 对全站非必要动效遵循 `prefers-reduced-motion`，并将编辑器图片选中态统一为青蓝品牌色。
- Playwright 已覆盖文章详情正文、匿名聊天输入与历史面板键盘交互、减少动效偏好，以及未认证后台访问重定向。
- 后台 E2E 使用当前 Supabase 项目的 service role 创建已确认邮箱的临时账号；临时文章的标题带唯一 `E2E cleanup` 前缀，并在草稿、预览、发布和删除验证结束后清理文章与账号。

剩余计划：

- 在 320 / 375 / 768 / 1024 / 1440px 与明暗主题下完成真实设备视觉和对比度验收。
- 已使用当前 Supabase 项目验证草稿保存、预览、发布和删除；每条自动化用例使用随机临时账号和唯一内容标识，并在 `finally` 与套件结束时兜底清理，不修改已有文章、碎片或账号。

预期收益：降低长期维护成本，减少跨页面重复实现和回归问题。

---

## 5. 核心组件优化建议

### Header

- 保持搜索为主要动作，主题切换为次要动作。
- 首页可展示页面标题或轻量欢迎语，二级页面应明确当前所在区域。
- 搜索框在中等宽度下可以收缩为按钮，但键盘快捷键行为应保持一致。
- 不在 Header 塞入天气、心情等与核心内容无关的信息。

### Sidebar

- 保留当前用户偏好的完整左侧栏形态，不改成只有图标的窄栏。
- 导航顺序围绕内容路径组织：首页、博客、归档、碎片、AI；管理入口和外部社交入口放在低优先级区域。
- 选中态使用青蓝浅底与主色文字，玫红不承担大面积导航状态。
- 保留站点头像、名称和一句短文案即可，避免再次强化“花园”概念。

### 卡片、列表与表格

- 首页只给 Featured 文章更强卡片层级，普通文章使用更轻的列表结构。
- 碎片允许更自由的视觉排布，但图片裁切和占位规则必须一致。
- 管理端表格优先可扫描性：稳定列宽、清晰状态、行级菜单和批量操作反馈。
- 所有异步内容需要 loading、empty、error 三种状态，不以空白代替失败反馈。

### 动效与交互

- hover 使用轻微位移、边框或背景变化，控制在 150–220ms。
- 避免首页大范围漂浮、粒子和无限循环装饰动画。
- 尊重 `prefers-reduced-motion`。
- 固定导航与播放器必须为正文预留安全间距，不能遮挡最后一项内容。

### 移动端

- 顶栏只保留品牌、搜索/主题等高频操作。
- 底部导航维持 4–5 个核心入口，触控区域不小于 44px。
- 首页桌面右栏在移动端改为按内容优先级自然插入，不机械压缩成三列。
- 音乐播放器和底部导航需要共享安全区计算，并适配 iOS safe area。

---

## 6. 功能迭代路线图

### P0：必须做，提升核心体验

#### 1. 全站视觉与导航一致性

- 用户价值：从首页进入任意页面时，交互和品牌体验保持连贯。
- 实现难度：中。
- 技术建议：复用公共布局与 tokens，分页面替换旧样式，不一次性重写业务逻辑。

#### 2. 搜索按需加载与结果体验

- 用户价值：更快找到文章和碎片，同时避免首页承担完整索引成本。
- 实现难度：中。
- 技术建议：搜索弹层打开后请求轻量索引，支持标题、摘要、标签和内容类型过滤。

#### 3. 碎片独立访问与分享

- 用户价值：单条碎片可以被定位、收藏和分享。
- 实现难度：中。
- 技术建议：优先使用可复制 URL 的详情页或拦截路由弹层，并通过 API 抽象层读取数据。

#### 4. AI 聊天状态透明化

- 用户价值：清楚知道匿名限制、模型状态、失败原因和可恢复操作。
- 实现难度：中。
- 技术建议：统一会话状态机和错误类型，保留现有匿名免费模型与限流规则。

#### 5. 编辑器可靠性

- 用户价值：降低长文编辑时内容丢失和误发布风险。
- 实现难度：中到高。
- 技术建议：增加本地草稿、保存状态、离开提醒和发布预览；继续以 TipTap JSON 为规范数据。

### P1：重要功能，提高竞争力

#### 1. 内容专题与关联阅读

- 用户价值：把零散文章和碎片组织为可持续浏览的主题路径。
- 实现难度：中。
- 技术建议：先基于现有标签计算关联，再考虑增加专题实体，避免过早修改数据库。

#### 2. 首页模块配置

- 用户价值：站主可以控制 Featured 内容、模块顺序和显示开关。
- 实现难度：中。
- 技术建议：先扩展 `site.config.json` 与现有 feature flags；需要动态管理时再评估数据库配置。

#### 3. AI 与站内内容联动

- 用户价值：AI 能基于已发布文章回答、推荐阅读和整理主题。
- 实现难度：高。
- 技术建议：服务端检索公开内容，输出来源链接；严格区分公开内容与后台草稿。

#### 4. PWA 与离线阅读

- 用户价值：提升移动端回访和弱网阅读体验。
- 实现难度：中。
- 技术建议：优先缓存应用壳层和已访问文章，不缓存私有会话及后台内容。

#### 5. 轻量访问分析

- 用户价值：了解真正被阅读的内容，用于优化首页推荐和写作方向。
- 实现难度：低到中。
- 技术建议：优先选择隐私友好的匿名统计，并提供明确隐私说明。

### P2：长期规划

#### 1. 独立桌面 Live2D 项目

- 用户价值：在更适合陪伴型交互的桌面环境中发挥 Live2D 价值，不干扰网站阅读。
- 实现难度：高。
- 技术建议：将现有 `src/lib/live2d/` 引擎能力抽离为独立包，桌面壳层可评估 Tauri；不要让桌面需求反向侵入网站公共布局。

#### 2. 多语言重新开放

- 用户价值：支持更广泛的阅读与内容分发。
- 实现难度：高。
- 技术建议：保留现有 i18n 架构，先解决文章翻译映射和后台编辑流程，再增加 locale。

#### 3. 内容导入、导出与备份

- 用户价值：保证个人内容长期可迁移和可恢复。
- 实现难度：中到高。
- 技术建议：定义 TipTap JSON、Markdown、媒体清单和元数据的版本化导出格式。

#### 4. 桌面端与网站内容同步

- 用户价值：桌面 Live2D 可以提示新文章、音乐和对话，但网站仍保持独立可用。
- 实现难度：高。
- 技术建议：通过受限 API 和独立鉴权同步，不直接复用 service role 或浏览器会话。

---

## 7. AGENTS.md 规则建议

现有规则整体适合后续开发，应继续遵守 API 抽象层、Supabase 客户端职责、Server Component 优先和 TipTap JSON 规范等约束。

`AGENTS.md` 已同步 Live2D 的保护规则：保留引擎与资源以支持未来的独立桌面项目，但网站公共布局不再默认挂载。

还可补充以下规则：

- UI 必须优先使用全局语义变量，不新增散落的品牌 hex 色值。
- 新组件必须提供加载、空数据和错误状态中的适用部分。
- 移动端固定导航和播放器必须验证 safe area 与正文底部间距。
- 新增公共页面需检查 320、375、768、1024、1440px 关键宽度。
- 用户可见稳定文案优先进入 `next-intl` 消息文件。
- 首页与列表不得展示没有真实数据来源的伪统计、伪状态或伪功能。

---

## 8. 推荐执行顺序

1. **已完成：** 建立基线、统一青蓝/玫红 tokens、保留可回退的 Git 检查点。
2. **已完成：** 重构桌面侧栏、公共页头、移动端顶部栏与底部导航。
3. **已完成：** 完成首页 Hero、Featured、碎片、文章列表、音乐、AI 和统计模块的重新编排。
4. **已完成（待视觉确认）：** 使用真实内容完成首页边界状态、主题和可访问性收口，并迁移首页及公共壳层的稳定文案。
5. **已完成（待视觉确认）：** 统一博客列表、文章详情、归档和搜索，打通核心阅读链路。
6. **已完成（待视觉确认）：** 为碎片增加独立访问能力，并统一图片加载与错误降级。
7. **已完成（待视觉确认）：** 优化 AI 聊天、音乐播放器的状态反馈和外部服务降级。
8. **已完成（待视觉确认）：** 统一登录、后台工作台和草稿箱，并加强编辑器的保存、离开提醒与连续编辑可靠性。
9. **已完成（待视觉确认）：** 完成可访问性基础收口，并将端到端测试扩展到文章阅读、匿名 AI 聊天、减少动效偏好和未认证后台保护。
10. **已完成（待人工视觉确认）：** 在当前 Supabase 项目内通过可清理的临时账号和文章验证草稿、预览、发布与删除；下一步进行多尺寸/明暗主题人工验收。

当前不建议先增加新的首页装饰模块，也不建议在全站视觉尚未收敛前扩张数据库结构。

---

## 9. 验收标准

### 首页与公共布局

- 桌面端侧栏、页头、主内容和右栏无重叠，1024px 下仍可用。
- 320px 和 375px 下无横向滚动，底部导航不遮挡内容。
- 首页在无文章、无碎片、无封面、长标题和音乐失败时仍保持完整布局。
- 亮色和深色主题下文本、边框、主色与玫红强调均有足够对比度。
- 键盘可以访问导航、搜索、主题切换、文章和音乐控制。
- 减少动态效果时不出现依赖动画才能理解的状态。

### 工程质量

- `pnpm run lint` 无新增错误。
- TypeScript 检查通过。
- `pnpm run test` 通过，并为新增状态逻辑补充必要测试。
- `pnpm run build` 在可访问外部依赖的环境中通过。
- 当前 UI 阶段不修改数据库 schema，不绕过 `src/lib/api/` 数据抽象层。
- 不修改 `.env*`，不提交密钥，不自动执行数据库迁移。

---

## 10. 当前结论

项目首页已不再处于“待设计”阶段：新的公共骨架和首页信息架构已经落地，方向是暖中性画布、冷青蓝主色、少量玫红强调，以及以真实文章和现有功能为中心的个人内容空间。

接下来的重点不是继续堆叠首页模块，而是完成真实数据和移动端边界验证，并把同一套视觉语言落实到可访问性和持续质量保障。AI 与音乐的状态反馈和外部服务降级已完成；编辑器已具备保存、离开保护、发布预览和图片恢复，首页、搜索、博客、登录与移动导航已有生产模式端到端基线。认证路径自动化验证已补齐，下一步独立推进桌面 Live2D 项目，并完成真实设备的多尺寸与明暗主题人工验收。

## 11. 阶段 10 验证记录（2026-07-27）

- 后台端到端用例直接使用当前 Supabase 项目，但只创建带 `everythingflows-e2e-` 前缀的临时账号与带 `E2E cleanup` 前缀的临时文章；草稿、预览、发布和删除完成后立即清理，并在套件结束时再次兜底清理。
- 发布完成后采用整页导航进入公开文章，避免后台工作台与公开页面在缓存失效期间发生客户端路由停滞。
- 旧 Supabase `post-images` 桶及其 30 个对象已删除；文章和碎片中旧存储 URL 的引用已归零，后续媒体仅使用 R2。
- 已在 Chromium 生产构建中检查首页 320 / 375 / 768 / 1024 / 1440px，以及博客、文章和碎片在 320 / 1024px 的亮色、暗色主题；均无横向溢出。审计中修复了 768px 同时出现桌面侧栏与移动播放器的断点不一致问题。
- Playwright 已通过后台认证 2 条、公共页面 7 条与视觉回归 2 条。仍建议在真实 iOS/Android 设备上确认刘海屏 safe area、系统字体和触摸手感。
